apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml

namePrefix: prod-
commonLabels:
  environment: prod

replicas:
  - name: guestbook
    count: 3
